أريد عند استنساخ أكواد مشروع ريبلت وتحويلها إلى Flutter أن تنتبه للنقاط التالية:

حافظ على نفس التصميم والوظائف الموجودة في كود ريبلت (UI + منطق البيانات).

عند تحويل الكود:

اجعل كل واجهة React تتحول إلى صفحة Flutter (Widget/Screen).

لا تنقل أي كود زائد غير مستخدم.

تجنب الأخطاء الشائعة:

لا تضع Expanded أو Flexible في أماكن خاطئة (مثل داخل ListView أو SingleChildScrollView).

لا تستخدم const مع قيم ديناميكية (مثلاً نصوص تأتي من API).

لا تترك أي Future بدون await أو then.

لا تخلط بين StatelessWidget و StatefulWidget إذا كانت الصفحة تحتاج تحديث البيانات.

بالنسبة للـ API:

نفس الاستدعاءات في ريبلت (login, dashboard, invoices, usage, services, routers) يجب أن تبقى نفسها في Flutter داخل ملف واحد (api_service.dart).

البيانات المستلمة JSON → لازم تتحول إلى Models في Dart (Classes) بدل استخدام dynamic.

حافظ على RTL واللغة العربية كما هو في تصميم Flutter.

كل تعديل تعمله لازم يكون مكتمل (صفحة كاملة أو وظيفة كاملة) وليس مجرد كود ناقص.

الهدف: أن يكون التطبيق Flutter مطابق 100% لتطبيق ريبلت من حيث الشكل والوظائف، لكن مكتوب بلغة Dart/Flutter وبأفضل الممارسات.

✅ 1. lib/api/api_service.dart

الوظائف المطلوبة:

loginUser(username, password)

getDashboardData(username)

getInvoices(username)

getUsageData(username)

getServices(username)

getRoutersData(username)

logout()

isLoggedIn()

getSavedUsername()

ملاحظات:

كل الدوال تستدعي API من السيرفر بنفس طريقة ريبلت.

يرجع Map أو List<Map> بدل dynamic.

headers: 'Content-Type': 'application/x-www-form-urlencoded'.

✅ 2. lib/pages/login_page.dart

المطلوب:

حقول: اسم المستخدم + كلمة المرور.

زر "تسجيل الدخول".

عند النجاح: حفظ المستخدم في SharedPreferences + الانتقال للـ Dashboard.

ملاحظات:

نفس ألوان ريبلت (أزرق/أبيض).

اتجاه RTL.

✅ 3. lib/pages/dashboard_page.dart

المطلوب:

عرض بيانات من getDashboardData:

اسم المستخدم.

اسم الباقة + تفاصيلها.

البيانات المتبقية (progress bar).

تاريخ الانتهاء.

حجم الرفع/التحميل.

حالة الاشتراك (فعال / منتهي).

ملاحظات:

استخدام بطاقات (Cards) مثل ريبلت.

نفس شريط العنوان + الشعار.

✅ 4. lib/pages/invoices_page.dart

المطلوب:

جلب بيانات من getInvoices.

فلتر (الكل – مدفوعة – غير مدفوعة).

عرض كل فاتورة:

رقم الفاتورة، السعر، الحالة (Badge خضراء/حمراء).

الخدمة + المدير + تاريخ الإصدار + تاريخ الدفع.

ملخص أسفل الصفحة:

إجمالي الفواتير.

المدفوعة.

غير المدفوعة.

ملاحظات:

نسخ نفس تصميم ريبلت (Cards + Filter Buttons).

استخدام RefreshIndicator للسحب للتحديث.

✅ 5. lib/pages/usage_page.dart

المطلوب:

عرض تقارير:

الاستخدام اليومي.

الاستخدام الشهري.

الاستخدام السنوي.

استخدام Charts + Cards مثل ريبلت.

ملاحظات:

الألوان والـ RTL مثل ريبلت.

✅ 6. lib/pages/services_page.dart

المطلوب:

عرض جميع الخدمات المتاحة (الباقات).

لكل باقة: اسمها + السعر + السرعة + الحجم.

زر "شراء" أو "تجديد" الباقة.

ملاحظات:

عند الضغط → استدعاء API يرسل إشعار للمدير (مثل ريبلت).

✅ 7. lib/pages/routers_page.dart

المطلوب:

شاشة إعداد الراوتر.

حقول لتغيير اسم الشبكة (SSID) وكلمة المرور.

زر "حفظ" يرسل الطلب عبر API.

ملاحظات:

دعم موديل TP-Link TL-WR840N (نفس اللي مستخدم في السيرفر).

✅ 8. lib/main.dart

المطلوب:

AuthWrapper: التحقق من تسجيل الدخول.

MainNavigationWrapper: يحتوي شريط تنقل سفلي (BottomNavigationBar):

التقارير – الخدمات – الرئيسية – الفواتير – الراوتر.

RTL مفعل.

theme بالأزرق/الأبيض.

📌 تعليمات عامة للوكيل

أي صفحة Flutter لازم تطابق React في ريبلت شكلًا ووظيفة.

كل استدعاء API يتم عن طريق api_service.dart فقط.

RTL واللغة العربية أساسي.

لا تترك أي كود ناقص (كل صفحة مكتملة).

احرص أن التصميم والـ UI يطابق تمامًا ريبلت (Cards، Badges، Filters، Progress).